{% extends "indexadmin.html.twig" %}
{% set navb = 5 %}

{% block titre %}Liste des images
{% endblock %}

{% block content %}
<div class='container'>
	<div class='row'>
		{% for file in files %}
			<div class='col-auto mb-3'>
				<div class="card text-center galerie">
					{% if file.href %}
						<a href="{{file.href}}" target="_blank">
							<img src="{{asset('/uploads/' ~ file.name) }}" class="card-img-top taille" alt="{{file.name}}">
						</a>
					{% else %}
						<img src="{{asset('/uploads/' ~ file.name) }}" class="card-img-top taille" alt="{{file.name}}">
					{% endif %}
					<div class="card-body">
						{% if file.href %}
							<a href="{{file.href}}" target="_blank">
								{{file.name}}
							</a>
						{% else %}
							{{file.name}}
						{% endif %}
					</div>
					<div class="card-footer">
						{% if file.del %}
							<button class="btn btn-link text-danger" data-bs-toggle="modal" data-bs-target="#del{{file.numb}}">
								<i class="far fa-trash-alt"></i>
							</button>
							<!-- Modal Validation de suppression !!!!!!!! -->
							<div class="modal fade" id="del{{file.numb}}" tabindex="-1" role="dialog" aria-labelledby="del" aria-hidden="true">
								<div class="modal-dialog" role="document">
									<div class="modal-content">
										<div class="modal-body">
											<h6 class='modal-title'>{{file.name}}</h6>
											<h6 class='modal-body'>Etes-vous sûr de vouloir la supprimer ?</h6>
										</div>
										<div class="modal-footer">
											<button type="button" class="btn btn-success btn-sm" data-bs-dismiss="modal">
												Annuler</button>
											<a href="/admin/delimage/{{file.name}}" type="button" class="btn btn-danger btn-sm">
												Supprimer !!</a>
										</div>
									</div>
								</div>
							</div>
						{% else %}
							<p class='bg-success'>
								Image utiliser
							</p>
						{% endif %}
					</div>
				</div>
			</div>
		{% endfor %}
	</div>
</div>
{% endblock %}

{% block footer %}
	<div>
		Page 1
	</div>
{% endblock %}
